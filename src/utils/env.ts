export function required(name:string):string { const v=process.env[name]; if(!v) throw new Error(`Missing required environment variable: ${name}`); return v; }
export function credentials(envName:string):Record<string,string>{ const raw=required(envName); let parsed:unknown; try{parsed=JSON.parse(raw)}catch{throw new Error(`Environment variable ${envName} is not valid JSON`)} if(!parsed||typeof parsed!=="object"||Array.isArray(parsed)) throw new Error(`Environment variable ${envName} must be a JSON object`); for(const [k,v] of Object.entries(parsed as Record<string,unknown>)) if(typeof v!=="string"||!v) throw new Error(`Credential ${k} in ${envName} must be a non-empty string`); return parsed as Record<string,string>; }
