import { proxySettingsSchema, type ProxySettings } from '../schemas/config.js';
export function required(name:string):string { const value=process.env[name]; if(!value) throw new Error(`Missing required environment variable: ${name}`); return value; }
export function keyArray(name:string):string[]{ const raw=required(name); let value:unknown; try{value=JSON.parse(raw)}catch{throw new Error(`Environment variable ${name} is not valid JSON`)} if(!Array.isArray(value)||value.length===0||value.some(x=>typeof x!=='string'||x.trim()==='')) throw new Error(`Environment variable ${name} must be a non-empty JSON array of non-empty strings`); return value as string[]; }
export function proxySettings():ProxySettings|undefined { const raw=process.env.PROXY_SETTINGS; if(!raw) return undefined; let value:unknown; try{value=JSON.parse(raw)}catch{throw new Error('PROXY_SETTINGS is not valid JSON')} return proxySettingsSchema.parse(value); }
