import test from 'node:test'; import assert from 'node:assert/strict'; import {createServer} from 'node:http'; import {createClient} from '../src/adapters/omniroute/client.js';
test('waitReady polls the real health ping endpoint',async()=>{let count=0;const server=createServer((req,res)=>{if(req.url==='/api/health/ping'){count++;res.statusCode=count<2?503:200;res.end('{}');return}res.statusCode=404;res.end();});await new Promise<void>(resolve=>server.listen(0,'127.0.0.1',resolve));const port=(server.address() as any).port;await createClient(`http://127.0.0.1:${port}`).waitReady(3000);assert.equal(count,2);server.close();});
