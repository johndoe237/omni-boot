import test from 'node:test'; import assert from 'node:assert/strict'; import {OmniRouteAdapter} from '../src/adapters/omniroute/index.js';
test('translates models and combo references without implementing routing',async()=>{const calls:any[]=[];const client:any={request:async(path:string,init:any)=>{calls.push({path,body:JSON.parse(init.body)});return {id:'combo-runtime'}},waitReady:async()=>{}};const adapter=new OmniRouteAdapter(client);await adapter.createCombo({name:'coding',strategy:'priority',targets:{models:['router/openai/gpt-5.6-luna'],combos:['free-combo']}},{providers:{router:'provider-runtime'},connections:{},combos:{},proxies:{}});assert.deepEqual(calls[0],{path:'/api/combos',body:{name:'coding',strategy:'priority',models:[{kind:'model',providerId:'provider-runtime',provider:'router',model:'openai/gpt-5.6-luna'},{kind:'combo-ref',comboName:'free-combo'}]}});});
